---
title: Installation
description: Install the Jiang compiler.
---

Jiang 0.4.9 provides a prebuilt macOS arm64 compiler package. This is the recommended way to try the language today.

## Requirements

- macOS on arm64.
- A host C compiler available as `cc` for final executable linking.

The release compiler statically links the LLVM libraries used by Jiang. You do not need a separate
`libLLVM.dylib` installation to run the packaged compiler.

## Install a Release

Download `jiang-0.4.9-macos-arm64.zip` from the
[0.4.9 GitHub release](https://github.com/jjcfun/jiang/releases/tag/0.4.9), then unpack it.

Install Jiang:

```bash
./install.sh
```

By default this installs:

```text
~/.jiang/versions/0.4.9
~/.jiang/bin/jiangc
```

Add Jiang to your shell path if needed:

```bash
export PATH="$HOME/.jiang/bin:$PATH"
```

## Verify

Check the compiler and LLVM runtime:

```bash
jiangc --version
```

The first line should be:

```text
jiang 0.4.9
```

## Build From Source

Source builds are mainly for compiler development. Clone the repository:

```bash
git clone https://github.com/jjcfun/jiang.git
cd jiang
```

Install LLVM:

```bash
bash ./script/install_llvm.sh
```

The script installs Jiang's managed LLVM 22 toolchain under
`$JIANG_HOME/toolchains/llvm/<version>/<host>`. If `JIANG_HOME` is not set, `~/.jiang` is used.

Build and verify the compiler with the installed 0.4.9 release:

```bash
BOOTSTRAP_RELEASE_VERSION=0.4.9 \
BOOTSTRAP_BIN="$HOME/.jiang/versions/0.4.9/bin/jiangc" \
BOOTSTRAP_DEPTH=stable \
VERIFY=full \
bash ./script/build_next.sh
```

## Next Step

Continue with [Getting Started](/getting-started/) to compile a small program.
