---
title: 安装
description: 安装 Jiang 编译器。
---

Jiang 为 macOS arm64 和 Linux x86_64 提供预构建编译器包。

## 环境要求

- macOS arm64 或 Linux x86_64。
- 宿主系统中可用的 `cc`，用于最终链接可执行文件。

发布包中的编译器已经静态链接 Jiang 使用的 LLVM 库；运行发布包不需要单独安装
LLVM 动态库。Linux 发布包要求 glibc 2.34 或更新版本。

## 安装发布包

从[最新 GitHub 发布页](https://github.com/jjcfun/jiang/releases/latest)下载当前 host 对应的归档，然后解压。

安装 Jiang：

```bash
./install.sh
```

默认安装到：

```text
~/.jiang/versions/<version>
~/.jiang/bin/jiangc
```

如果需要，把 Jiang 加入 shell 的 PATH：

```bash
export PATH="$HOME/.jiang/bin:$PATH"
```

## 验证

检查编译器和 LLVM 运行时：

```bash
jiangc --version
```

第一行应该是：

```text
jiang <version>
```

## 从源码构建

源码构建面向编译器贡献者。其工具链和 bootstrap 要求会随编译器演进；需要从源码构建时，请阅读
[编译器开发流程](https://github.com/jjcfun/jiang/blob/release/0.5.2/doc/develop.md)。

普通语言使用者不需要从源码构建编译器。

## 下一步

继续阅读[快速开始](/zh/getting-started/)，编译一个小型 Jiang 程序。
